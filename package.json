{
  "name": "credit-report-parser",
  "private": true,
  "description": "Root scripts; customer UI lives in web/",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "test:e2e": "npm run test:e2e --prefix web",
    "workflow-api": "python -m uvicorn api.workflow_app:app --host 127.0.0.1 --port 5000"
  }
}
